PARCIAL: los 12 rubros auditados y calificados con contexto fresco, tablero pintado y MIRADO (captura en `tablero.png`), 2 de 13 críticos cerrados con prueba que reproduce en rojo — DAT-C1 (`285d5e3`) y FI-C1 (`86fb450`) —, y los 11 restantes PENDIENTES con su escenario y su `archivo:línea` escritos. Altos, medios y bajos quedan propuestos.

Ronda de **CONTINUACIÓN** sobre `claude/auditoria-3` / **PR #13**. No se abrió PR nuevo: el PR seguía abierto y ya hay cinco más de rondas viejas encimados. El pase empezó mergeando `master` (81 commits, 380 archivos, +54,356 líneas) a la rama, y por eso se relanzaron los DOCE rubros y no tres: los reportes del pase 2 describían un árbol que ya no existe.

Global 5.3 → 5.4 en recolección (65/12), 5.5 al cierre. Siete rubros suben, tres bajan. Lo que el promedio esconde: agéntico ▼2 y arquitectura ▼1 son el precio de meter 380 archivos en siete días, y rendimiento lleva TRES rondas clavado en 4 con la misma familia de fallos intacta (caminos sin reloj que exceden su `maxDuration`).

Compuerta verde sobre el árbol final: `npx vitest run` 331 archivos / 4,509 verdes / 1 skip (exit 0) · `npx tsc --noEmit -p .` limpio · `npm run lint` 0 errores, 23 warnings. `npm run build` no aplica en la nube.

Qué faltó y por qué: con 13 críticos sobre la mesa se prefirió cerrar dos DE VERDAD —prueba roja primero, arreglo, verde, suite completa, commit atómico— antes que dejar seis a medias. El pase 1 de esta misma ronda dio 6 críticos por cerrados sin ancla real y el pase 2 encontró cuatro de esas rutas todavía abiertas; repetir ese patrón costaba más de lo que valía. Los dos elegidos son los que rompen una regla dura del producto: uno hacía que la pantalla de estrategia de agentes no pudiera guardar NUNCA, el otro imprimía una elegibilidad fiscal equivocada citando una regla verificada en fuente primaria.

INFRA (no son hallazgos del código): `npm ci` NO corre en este contenedor — `package.json:38` pide `xlsx` desde `cdn.sheetjs.com` y la política de red deniega ese host (403 en el CONNECT), npm revierte y deja `node_modules/` vacío. Se instaló `xlsx@0.18.5` desde el registry de npm solo para poder correr la compuerta, y `package.json`/`package-lock.json` quedaron RESTAURADOS y sin commitear. El clon llegó shallow (depth 50) y con HEAD detached.
