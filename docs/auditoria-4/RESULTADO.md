PARCIAL: los 12 rubros auditados y calificados con contexto fresco, tablero pintado y MIRADO (captura en `tablero.png`), los 15 CRÍTICOS verificados uno por uno abriendo el archivo (0 descartados por falsos — primer pase del ciclo en que eso pasa), 2 de 15 cerrados con prueba que reproduce en rojo — A4-DAT-C1 (`dd65f6c`) y A4-FIS-C1 (`5394d10`) — y los 13 restantes PENDIENTES con su escenario y su `archivo:línea` escritos. Altos, medios y bajos quedan propuestos.

Ronda de **CONTINUACIÓN** sobre `claude/auditoria-3` / **PR #13**, pase 4. No se abrió PR nuevo: el PR seguía abierto y ya hay cinco de auditorías viejas encimados. El pase empezó mergeando `master` (37 commits, 110 archivos, +9,355 líneas, migs 0112→0120) a la rama, y por eso se relanzaron los DOCE rubros y no tres: los reportes del pase 3 describían un árbol que ya no existe.

Global **5.5 → 4.8** (▼0.7). Diez rubros bajan o se quedan; uno sube. Lo que el promedio esconde: no fue una semana mala, fue una semana buena cuya deuda cobró factura. Los abiertos del pase 3 no se movieron (frontend 10/10, legal 11/13, datos 9/9, operabilidad sus dos críticos, rendimiento los cuatro), el patrón "sin reloj" que lleva cuatro rondas señalado se copió a los dos subsistemas escritos esta semana, y tool calling perdió su invariante estructural: eran 14 definiciones de tools y hoy son 27, nueve de ellas aceptando parámetros del modelo.

La compuerta arrancó **ROJA** y cerró **VERDE**. Al arrancar: `npx tsc --noEmit -p .` exit 1 (TS1117), y el CI de la rama llevaba en `failure` desde el merge sin que la suite verde lo pudiera ver. Al cerrar sobre el árbol final: `npx vitest run` 349 archivos / 4,657 verdes / 1 skip (exit 0) · `npx tsc --noEmit -p .` limpio · `npm run lint` 0 errores, 0 warnings. `npm run build` no aplica en la nube.

Qué faltó y por qué: con 15 críticos sobre la mesa se prefirió cerrar dos DE VERDAD —prueba roja primero, arreglo, verde, suite completa, commit atómico— antes que dejar seis a medias. A4-OP-C2 quedó a un paso y se dejó PENDIENTE a propósito: el arreglo es de una línea y el patrón correcto ya vive en `escalar_viaje.ts:392`, pero reproducirlo en rojo exige conducir el mock de Supabase de `cobranza_global.test.ts` hasta 40 contactos fallidos sin excepción, y eso no cabía en el turno. No se arregla lo que no se pudo reproducir.

INFRA (no son hallazgos del código): `npm ci` NO corre en este contenedor, tercera ronda consecutiva — `package.json:40` pide `xlsx` desde `cdn.sheetjs.com` y la política de red deniega ese host (403 en el CONNECT, verificado con `curl` esta corrida). Se instaló `xlsx@0.18.5` desde el registry solo para poder correr la compuerta, y `package.json`/`package-lock.json` quedaron RESTAURADOS y sin commitear.
